#git init - initializes a repository, only do once.
#git status - shows status of current git repo.
#git log - shows history of commits.
#git branch - creates a new branch.
#git checkout - switch to a new branch.
#touch - add a new file.
#git commit - takes a snapshot of the current state of the code.
#git add - sends files to staging area or adds them to the index.
#git config - set configurations values of git.
#local repos are on your computer, remote repos live on the cloud.
#make local branch and git branch the same name.
